const ErrorMessage = ({ message }) => {
    return (
        <div className="mx-auto">
            <h5 className="justify-content-center">{message}</h5>
        </div>
    );
};

export default ErrorMessage;